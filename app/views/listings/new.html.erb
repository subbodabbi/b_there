<div class="new-listing" >
  <h2> New Listing: </h2>

  <%= form_for @listing, remote: true, authenticity_token: true, :html => { :id => 'new-listing-form', :multipart => true  } do |form| %>
    <%= render partial: '/listings/form', object: form %>

    <div class="submit-field">
      <%= form.submit %>
    </div>
  <% end %>
</div>